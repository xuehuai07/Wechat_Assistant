DEEPSEEK_API_KEY=
WEB_PASSWORD=
AMAP_MAPS_API_KEY=
# Leave empty for local development. Set to 1 only when the backend should serve frontend/dist.
# Docker Compose sets this automatically.
APP_SERVE_FRONTEND=
